/**
 * Test stub for the `server-only` marker module.
 *
 * Next.js resolves `server-only` through its own bundler condition. Under
 * Vitest there is no such condition, so the import is aliased to this empty
 * module. The guarantee it provides still holds at build time.
 */
export {};
